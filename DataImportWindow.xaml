<Window x:Class="FSB_helper_C__.DataImportWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Import Data" Height="400" Width="400"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        WindowStartupLocation="CenterOwner" ShowInTaskbar="False" ResizeMode="NoResize"
        TextOptions.TextFormattingMode="Display" TextOptions.TextRenderingMode="ClearType">
    
    <Grid>
    <Grid Margin="15" VerticalAlignment="Center" HorizontalAlignment="Center">
        <!-- Shadow Layer -->
        <Border Background="{DynamicResource BgBrush}" CornerRadius="14">
            <Border.Effect>
                <DropShadowEffect BlurRadius="25" ShadowDepth="0" Color="#1f6feb" Opacity="0.4" RenderingBias="Performance"/>
            </Border.Effect>
        </Border>
        
        <!-- Content Layer -->
        <Border Width="360" Background="{DynamicResource BgBrush}" CornerRadius="14" BorderBrush="#1f6feb" BorderThickness="2" UseLayoutRounding="True" SnapsToDevicePixels="True">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="45"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <!-- Header bar -->
                <Border Background="{DynamicResource SideBrush}" CornerRadius="12,12,0,0">
                    <Grid Margin="20,0">
                        <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                            <TextBlock Text="⬇" Foreground="#1f6feb" FontSize="13" Margin="0,0,8,0" VerticalAlignment="Center"/>
                            <TextBlock Text="ИМПОРТ ДАННЫХ В СИСТЕМУ" Foreground="White" FontSize="13" FontWeight="Black" FontFamily="Segoe UI" VerticalAlignment="Center"/>
                        </StackPanel>
                        <Button Style="{StaticResource ModalCloseBtn}" Click="Close_Click"/>
                    </Grid>
                </Border>
                <Border Grid.Row="0" Height="1" VerticalAlignment="Bottom" Background="#30363d"/>

                <!-- Content -->
                <StackPanel Grid.Row="1" Margin="20,15,20,20">
                    <TextBlock TextWrapping="Wrap" Foreground="#8b949e" FontSize="11" FontFamily="Segoe UI" Margin="0,0,0,12">Выберите тип файла, который вы хотите загрузить. Текущие данные в выбранной категории будут перезаписаны.</TextBlock>

                    <!-- 1. Profile -->
                    <Button Height="52" Cursor="Hand" Click="ImportProfile_Click" Margin="0,0,0,8">
                        <Button.Template>
                            <ControlTemplate TargetType="Button">
                                <Border x:Name="bd" Background="#0c182e" BorderBrush="#1f6feb" BorderThickness="1.5" CornerRadius="8" Padding="12,0">
                                    <Grid>
                                        <Grid.ColumnDefinitions><ColumnDefinition Width="36"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions>
                                        <Grid>
                                            <Ellipse Width="26" Height="26" Fill="#1f6feb" Opacity="0.2"/>
                                            <Canvas Width="12" Height="10" HorizontalAlignment="Center" VerticalAlignment="Center">
                                                <Line X1="0" Y1="2" X2="12" Y2="2" Stroke="#1f6feb" StrokeThickness="2" StrokeStartLineCap="Round" StrokeEndLineCap="Round"/>
                                                <Line X1="0" Y1="5" X2="12" Y2="5" Stroke="#1f6feb" StrokeThickness="2" StrokeStartLineCap="Round" StrokeEndLineCap="Round"/>
                                                <Line X1="0" Y1="8" X2="12" Y2="8" Stroke="#1f6feb" StrokeThickness="2" StrokeStartLineCap="Round" StrokeEndLineCap="Round"/>
                                            </Canvas>
                                        </Grid>
                                        <StackPanel Grid.Column="1" VerticalAlignment="Center" Margin="8,0,0,0">
                                            <TextBlock Text="ПРОФИЛЬ ЦЕЛИКОМ" Foreground="White" FontSize="13" FontWeight="Bold" FontFamily="Segoe UI"/>
                                            <TextBlock Text="Файл (.duran). Загрузит бинды, законы и настройки." Foreground="#8b949e" FontSize="10" FontFamily="Segoe UI" Margin="0,2,0,0"/>
                                        </StackPanel>
                                    </Grid>
                                </Border>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter TargetName="bd" Property="Background" Value="#1f2937"/>
                                        <Setter TargetName="bd" Property="BorderBrush" Value="#58a6ff"/>
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Button.Template>
                    </Button>

                    <!-- 2. Laws only -->
                    <Button Height="52" Cursor="Hand" Click="ImportLaws_Click" Margin="0,0,0,8">
                        <Button.Template>
                            <ControlTemplate TargetType="Button">
                                <Border x:Name="bd" Background="{DynamicResource SideBrush}" BorderBrush="#30363d" BorderThickness="1.5" CornerRadius="8" Padding="12,0">
                                    <Grid>
                                        <Grid.ColumnDefinitions><ColumnDefinition Width="36"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions>
                                        <Grid>
                                            <Ellipse Width="26" Height="26" Fill="#d2a65e" Opacity="0.2"/>
                                            <Canvas Width="14" Height="10" HorizontalAlignment="Center" VerticalAlignment="Center">
                                                <Line X1="0" Y1="2" X2="14" Y2="2" Stroke="#d2a65e" StrokeThickness="2" StrokeStartLineCap="Round" StrokeEndLineCap="Round"/>
                                                <Line X1="0" Y1="5" X2="14" Y2="5" Stroke="#d2a65e" StrokeThickness="2" StrokeStartLineCap="Round" StrokeEndLineCap="Round"/>
                                                <Line X1="0" Y1="8" X2="9" Y2="8" Stroke="#d2a65e" StrokeThickness="2" StrokeStartLineCap="Round" StrokeEndLineCap="Round"/>
                                            </Canvas>
                                        </Grid>
                                        <StackPanel Grid.Column="1" VerticalAlignment="Center" Margin="8,0,0,0">
                                            <TextBlock Text="ТОЛЬКО БАЗА ЗАКОНОВ" Foreground="White" FontSize="13" FontWeight="Bold" FontFamily="Segoe UI"/>
                                            <TextBlock Text="Файл (.json). Загрузит только разделы УК, КоАП и т.д." Foreground="#8b949e" FontSize="10" FontFamily="Segoe UI" Margin="0,2,0,0"/>
                                        </StackPanel>
                                    </Grid>
                                </Border>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter TargetName="bd" Property="Background" Value="#1f2937"/>
                                        <Setter TargetName="bd" Property="BorderBrush" Value="#d2a65e"/>
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Button.Template>
                    </Button>

                    <!-- 3. Binds only -->
                    <Button Height="52" Cursor="Hand" Click="ImportBinds_Click" Margin="0,0,0,8">
                        <Button.Template>
                            <ControlTemplate TargetType="Button">
                                <Border x:Name="bd" Background="{DynamicResource SideBrush}" BorderBrush="#30363d" BorderThickness="1.5" CornerRadius="8" Padding="12,0">
                                    <Grid>
                                        <Grid.ColumnDefinitions><ColumnDefinition Width="36"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions>
                                        <Grid>
                                            <Ellipse Width="26" Height="26" Fill="#2ea043" Opacity="0.2"/>
                                            <Canvas Width="14" Height="10" HorizontalAlignment="Center" VerticalAlignment="Center">
                                                <Line X1="0" Y1="2" X2="14" Y2="2" Stroke="#2ea043" StrokeThickness="2" StrokeStartLineCap="Round" StrokeEndLineCap="Round"/>
                                                <Line X1="7" Y1="5" X2="7" Y2="10" Stroke="#2ea043" StrokeThickness="2" StrokeStartLineCap="Round" StrokeEndLineCap="Round"/>
                                            </Canvas>
                                        </Grid>
                                        <StackPanel Grid.Column="1" VerticalAlignment="Center" Margin="8,0,0,0">
                                            <TextBlock Text="ТОЛЬКО ИГРОВЫЕ БИНДЫ" Foreground="White" FontSize="13" FontWeight="Bold" FontFamily="Segoe UI"/>
                                            <TextBlock Text="Файл (.json). Загрузит бинды и группы." Foreground="#8b949e" FontSize="10" FontFamily="Segoe UI" Margin="0,2,0,0"/>
                                        </StackPanel>
                                    </Grid>
                                </Border>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter TargetName="bd" Property="Background" Value="#1f2937"/>
                                        <Setter TargetName="bd" Property="BorderBrush" Value="#2ea043"/>
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Button.Template>
                    </Button>

                    <!-- 4. Fines only -->
                    <Button Height="52" Cursor="Hand" Click="ImportFines_Click">
                        <Button.Template>
                            <ControlTemplate TargetType="Button">
                                <Border x:Name="bd" Background="{DynamicResource SideBrush}" BorderBrush="#30363d" BorderThickness="1.5" CornerRadius="8" Padding="12,0">
                                    <Grid>
                                        <Grid.ColumnDefinitions><ColumnDefinition Width="36"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions>
                                        <Grid>
                                            <Ellipse Width="26" Height="26" Fill="#8957e5" Opacity="0.2"/>
                                            <Canvas Width="14" Height="10" HorizontalAlignment="Center" VerticalAlignment="Center">
                                                <Line X1="2" Y1="10" X2="12" Y2="10" Stroke="#8957e5" StrokeThickness="2" StrokeStartLineCap="Round" StrokeEndLineCap="Round"/>
                                                <Line X1="2" Y1="10" X2="2" Y2="2" Stroke="#8957e5" StrokeThickness="2" StrokeStartLineCap="Round" StrokeEndLineCap="Round"/>
                                                <Line X1="2" Y1="2" X2="7" Y2="6" Stroke="#8957e5" StrokeThickness="2" StrokeStartLineCap="Round" StrokeEndLineCap="Round"/>
                                            </Canvas>
                                        </Grid>
                                        <StackPanel Grid.Column="1" VerticalAlignment="Center" Margin="8,0,0,0">
                                            <TextBlock Text="ТОЛЬКО БАЗА ШТРАФОВ" Foreground="White" FontSize="13" FontWeight="Bold" FontFamily="Segoe UI"/>
                                            <TextBlock Text="Файл (.json). Загрузит категории штрафов для калькулятора." Foreground="#8b949e" FontSize="10" FontFamily="Segoe UI" Margin="0,2,0,0"/>
                                        </StackPanel>
                                    </Grid>
                                </Border>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter TargetName="bd" Property="Background" Value="#1f2937"/>
                                        <Setter TargetName="bd" Property="BorderBrush" Value="#8957e5"/>
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Button.Template>
                    </Button>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
    </Grid>
</Window>
