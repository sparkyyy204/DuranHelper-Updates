<Window x:Class="FSB_helper_C__.AhkImportWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Импорт с AHK" Height="480" Width="420"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        WindowStartupLocation="CenterOwner" ShowInTaskbar="False" ResizeMode="NoResize"
        TextOptions.TextFormattingMode="Ideal" TextOptions.TextRenderingMode="ClearType">

    <Grid Margin="15" VerticalAlignment="Center" HorizontalAlignment="Center">
        <!-- Shadow Layer -->
        <Border Background="{DynamicResource BgBrush}" CornerRadius="14">
            
        </Border>
        
        <!-- Content Layer -->
        <Border Width="380" Background="{DynamicResource BgBrush}" CornerRadius="14" BorderBrush="#2ea043" BorderThickness="2" UseLayoutRounding="True" SnapsToDevicePixels="True">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="45"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <!-- Header bar -->
                <Border Background="{DynamicResource SideBrush}" CornerRadius="12,12,0,0">
                    <Grid Margin="20,0">
                        <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                            <TextBlock Text="H" FontFamily="Consolas" Foreground="#2ea043" FontSize="14" Margin="0,0,8,0" VerticalAlignment="Center" FontWeight="Bold"/>
                            <TextBlock Text="ИМПОРТ ИЗ AUTOHOTKEY" Foreground="{DynamicResource TextBrush}" FontSize="13" FontWeight="Black" FontFamily="Segoe UI" VerticalAlignment="Center"/>
                        </StackPanel>
                        <Button Style="{StaticResource ModalCloseBtn}" Click="CloseButton_Click"/>
                    </Grid>
                </Border>
                <Border Grid.Row="0" Height="1" VerticalAlignment="Bottom" Background="#30363d"/>

                <!-- Content Zone -->
                <Grid Grid.Row="1" Margin="20,15,20,20">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <!-- Title and Summary -->
                    <StackPanel Grid.Row="0" Margin="0,0,0,15">
                        <TextBlock Text="ПОДТВЕРЖДЕНИЕ ИМПОРТА" FontSize="14" Foreground="{DynamicResource TextBrush}" FontWeight="Bold" HorizontalAlignment="Center" Margin="0,0,0,5"/>
                        <TextBlock x:Name="ConfirmText" Text="Будет импортировано X новых биндов:" FontSize="12" Foreground="{DynamicResource GrayBrush}" HorizontalAlignment="Center" />
                    </StackPanel>

                    <!-- List of Binds -->
                    <Border Grid.Row="1" Background="{DynamicResource DeepBgBrush}" BorderBrush="#30363d" BorderThickness="1.5" CornerRadius="8" Margin="0,0,0,15" Padding="5">
                        <ScrollViewer VerticalScrollBarVisibility="Auto" PanningMode="VerticalOnly">
                            <ItemsControl x:Name="BindsList">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <Border Background="{DynamicResource SideBrush}" CornerRadius="6" Margin="4,4,4,4" Padding="12,10">
                                            <StackPanel>
                                                <TextBlock Text="{Binding name}" Foreground="{DynamicResource TextBrush}" FontSize="12" FontWeight="Bold" FontFamily="Segoe UI" Margin="0,0,0,4"/>
                                                <TextBlock Text="{Binding description}" Foreground="#2ea043" FontSize="10" FontFamily="Segoe UI" TextWrapping="NoWrap" TextTrimming="CharacterEllipsis"/>
                                            </StackPanel>
                                        </Border>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </ScrollViewer>
                    </Border>

                    <!-- Warning Text -->
                    <Border Grid.Row="2" Background="{DynamicResource DeepBgBrush}" BorderBrush="#2ea043" BorderThickness="1" CornerRadius="6" Padding="10" Margin="0,0,0,15">
                        <StackPanel>
                            <TextBlock Text="ВАЖНО:" Foreground="#2ea043" FontWeight="Bold" FontSize="11" Margin="0,0,0,3" />
                            <TextBlock Text="Вам нужно будет назначить клавиши и переименовать их." Foreground="{DynamicResource GrayBrush}" FontSize="10" Margin="0,1" />
                        </StackPanel>
                    </Border>

                    <!-- Action Buttons -->
                    <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Center">
                        <Button Content="ИМПОРТИРОВАТЬ" Width="140" Height="36" Background="#0f291e" Foreground="#2ea043" BorderBrush="#2ea043" BorderThickness="1.5" Style="{StaticResource FlatBtn}" FontSize="12" FontWeight="Bold" Cursor="Hand" Click="ImportButton_Click" Margin="0,0,10,0">
                            <Button.Resources><Style TargetType="Border"><Setter Property="CornerRadius" Value="6"/></Style></Button.Resources>
                        </Button>
                        <Button Content="ОТМЕНА" Width="120" Height="36" Background="{DynamicResource DeepBgBrush}" Foreground="{DynamicResource GrayBrush}" BorderBrush="#30363d" BorderThickness="1.5" Style="{StaticResource FlatBtn}" FontSize="12" FontWeight="Bold" Cursor="Hand" Click="CancelButton_Click" >
                            <Button.Resources><Style TargetType="Border"><Setter Property="CornerRadius" Value="6"/></Style></Button.Resources>
                        </Button>
                    </StackPanel>
                </Grid>
            </Grid>
        </Border>
    </Grid>
</Window>
